现在要为 TTS 提供商 "{{provider_name}}" 从用户要求中提取/选择合成参数。

【用户要求】
{{user_request}}

【可用参数定义（只允许使用这些字段）】
{{schema_text}}

【输出要求】
1) 只输出 JSON 对象（dict），不要 markdown，不要多余文本。
2) 只能输出可用参数定义里的字段；不要杜撰字段。
3) 值必须符合 type：
   - "int"/"float" 输出数字类型
   - "str" 输出字符串
   - "bool" 输出 true/false
4) enum 表示必须从列表中选一个最符合用户要求的值。
5) range 表示区间，输出一个落在区间内的数（可以保留 1 位小数）。
6) 用户没提到的字段可以不输出；但如果用户明确要求（例如性别/音色/语速/音量），**尽量输出对应字段**。